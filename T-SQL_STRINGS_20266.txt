 ASCII
SELECT ASCII ( 'n' ) 

CHAR
SELECT CHAR ( 110 )

LEFT
USE ETG_TRAIN
SELECT LEFT(ename, 3)   
FROM EMP

CONCAT
USE ETG_TRAIN
SELECT CONCAT( ename ,empno)
FROM EMP

RIGHT
USE ETG_TRAIN
SELECT RIGHT(ename, 3)   
FROM EMP

LOWER
USE ETG_TRAIN
SELECT LOWER(ename)   
FROM EMP

UPPER
USE ETG_TRAIN
SELECT UPPER(ename)   
FROM EMP

LEN
USE ETG_TRAIN
SELECT LEN(ename)   
FROM EMP

REVERSE
USE ETG_TRAIN
SELECT REVERSE(ename)   
FROM EMP

LTRIM
SELECT LTRIM('     HI')
SELECT LTRIM('HELLO       ')

SUBSTRING
USE ETG_TRAIN
SELECT M = SUBSTRING(ENAME, 2, 3)
FROM EMP

REPLACE
USE ETG_TRAIN
SELECT REPLACE(JOB,'SALESMAN','xxx')
FROM EMP

REPLICATE
USE ETG_TRAIN
SELECT REPLICATE(JOB,4) AS JOBS
FROM EMP